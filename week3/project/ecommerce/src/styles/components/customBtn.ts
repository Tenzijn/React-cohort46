import { defineStyleConfig } from '@chakra-ui/react';

export const customBtn = defineStyleConfig({
  baseStyle: {},
  sizes: {},
  variants: {
    'category-active-btn': {
      bg: 'rgb(54, 54, 54)',
      color: 'white',
    },
  },
  defaultProps: {},
});
